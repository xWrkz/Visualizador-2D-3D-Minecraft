const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', error => console.log('PAGE ERROR:', error.message)); await page.goto('http://localhost:5173'); await page.waitForSelector('.catalog-card'); console.log('Clicking card...'); await page.click('.catalog-card'); await new Promise(r => setTimeout(r, 4000)); console.log('Done'); await browser.close(); })(); 
